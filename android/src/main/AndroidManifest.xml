<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="io.rodcone.flutter_meta_wearables_dat">

  <!-- Background streaming: required by the foreground service started when
       the host app calls `MetaWearablesDat.enableBackgroundStreaming`. These
       are merged into the consumer app's manifest automatically. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <!-- Android 13+ (API 33+) requires a runtime grant of POST_NOTIFICATIONS for
       the foreground-service notification to appear in the notification shade.
       The plugin prompts for this when `enableBackgroundStreaming` is called. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application>
    <service
      android:name=".BackgroundStreamingService"
      android:enabled="true"
      android:exported="false"
      android:foregroundServiceType="connectedDevice" />
  </application>
</manifest>
